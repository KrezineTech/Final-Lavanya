import React from 'react';

interface ImageGalleryFormProps {
  onSubmit?: (data: any) => void;
}

export default function ImageGalleryForm({ onSubmit }: ImageGalleryFormProps) {
  return (
    <div>
      {/* TODO: Implement image gallery form */}
    </div>
  );
}
